{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="block-sm">
        <form method="post">
         {% for message in app.flashes('notice') %}
            <div class="alert alert-info">{{ message }}</div>
            {% endfor %}
            <h1 class="h3 mb-3 font-weight-normal">Réinitialiser mon mot de passe</h1>
            <label for="inputEmail">Email</label>
            <input type="email"  name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
            <button class="btn-block btn-lg btn-info mt-3" type="submit">
                Valider
            </button>
            <hr>
            <p class="text-center">ou souhaitez vous :<a href="{{ path('app_login') }}">se connecter?</a></p>

        </form>
    </div>
{% endblock %}
