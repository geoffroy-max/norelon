{% extends 'base.html.twig' %}

{% block title %}Mon  produit!{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-5">
        <img src="{{ vich_uploader_asset(product, 'imageFile') }}" alt="{{ product.name }}" class="img-fluid">
    </div>
    <div class="col-md-7 my-auto">
        <h3>{{ product.name }}</h3>
        <P>{{ product.subtitle }}</P>
         <span class="text-primary"> {{ (product.price/100)|number_format(2,',','') }}€</span>
        <hr>
        <P>{{ product.description }}</P>
        <a href="{{ path('cart_add',{'id':product.id}) }}" class="btn btn-primary">Ajouter un panier</a>
    </div>
</div>



{% endblock %}
